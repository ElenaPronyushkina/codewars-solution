// https://www.codewars.com/kata/583af10620dda4da270000c5

function mergeArrays(a, b) {
  let arr = [];
  for (let i = 0; i < Math.max(a.length, b.length); i++){
    arr = arr.concat(a.slice(i,i+1), b.slice(i,i+1));
  }
   return arr;
}
            
