//https://www.codewars.com/kata/588a7d45019c42be61000009

function interweave(s1, s2) {
  let res = '';
  for (let i = 0; i < s1.length; i++){
    res += s1.slice(i, i+1) + s2.slice(i, i+1)
  } 
  return res.replace(/[0-9]/g, '');
}
