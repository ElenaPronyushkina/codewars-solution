// https://www.codewars.com/kata/595aa94353e43a8746000120

function findDeletedNumber(arr, mixArr) {
  let sum1 = arr.reduce((a, b) => a + b, 0);
  let sum2 = mixArr.reduce((a, b) => a + b, 0);
  return sum1 - sum2;
}
