//https://www.codewars.com/kata/5f55ecd770692e001484af7d

function mirror(data) {
  let arr = data.concat().sort((a, b) => a - b);
  let arr1 = arr.concat().reverse();
    arr.pop();
  return arr.concat(arr1);
}
